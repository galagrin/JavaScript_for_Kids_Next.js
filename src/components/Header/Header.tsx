import Link from 'next/link';

import { NavigationMenu } from '../NavigationMenu/NavigationMenu';

export const Header = () => {
    return (
        <div>
            <NavigationMenu />
        </div>
    );
};
